# 贡献指南

感谢你帮助改进项目。提交前请先阅读 [内容与版权政策](CONTENT_POLICY.md)、[隐私说明](PRIVACY.md) 和 [架构与开发说明](docs/架构与开发.md)。

## 提交范围

欢迎贡献可公开再分发的代码、测试、原创微型示例、文档和兼容性改进。所有贡献必须由你原创，或已取得足以公开再分发的授权；请在 Pull Request 中说明来源或授权依据。

不得提交真实试卷、答案、学生信息、课程音视频、内部文件、密钥或任何未经授权的第三方教学材料。请用原创的最小复现替代真实材料，并在提交前自行检查路径、日志和截图是否已脱敏。

## 本地检查

在受支持的 Python 环境中安装开发依赖后，运行：

```bash
python -m pip install -e '.[dev]'
python -m pytest -q
cekit release-audit
python -m build
```

`release-audit` 是公开表面检查，不代替贡献者对版权、授权、匿名化和事实准确性的判断。公开仓库的 GitHub Actions 会在 PR 上执行受保护的跨平台测试与 `release-audit`；本地通过仍不能替代当前提交的远端绿色检查。

## 提交过程

1. 为一项清晰、范围有限的改动创建分支。
2. 为行为改动先添加会失败的测试，再实现最小改动。
3. 运行与改动相关的测试和上述本地检查。
4. 在 Pull Request 中说明目的、测试证据、兼容性影响，以及原创性或授权确认。
5. 尊重 [行为准则](CODE_OF_CONDUCT.md)，并配合维护者的公开发行和安全审查。
